<?xml version="1.0" encoding="UTF-8" ?>
<!--
  FastDDS profile that disables shared-memory transport.

  By default ROS2 Humble's FastDDS uses shared memory (/dev/shm) for
  same-host communication.  When the publisher is inside a Docker container
  and the subscriber is on the host, they do not share /dev/shm even with
  network_mode: host, so messages are never received outside the container.

  This profile restricts transport to UDPv4 only.  With network_mode: host
  the container shares the host network namespace, so UDP loopback works
  transparently between container nodes and host ros2 CLI tools.

  Referenced by FASTRTPS_DEFAULT_PROFILES_FILE in docker-compose.yml.
-->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">

  <transport_descriptors>
    <transport_descriptor>
      <transport_id>udp_transport</transport_id>
      <type>UDPv4</type>
    </transport_descriptor>
  </transport_descriptors>

  <participant profile_name="udp_only" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>udp_transport</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
  </participant>

</profiles>
